/**
 * @deskhand/core
 *
 * Core types and utilities for Deskhand desktop AI agent.
 *
 * Usage:
 * import type { Session, Message, Artifact } from '@deskhand/core';
 * import { generateMessageId, debug } from '@deskhand/core';
 */

// Re-export all types
export * from './types/index.ts';

// Re-export utilities
export * from './utils/index.ts';
